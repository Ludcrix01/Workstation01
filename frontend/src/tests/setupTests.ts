import '@testing-library/jest-dom';

// Setup global fetch mock placeholder (tests may override per-case)
// global.fetch = jest.fn();

// Optionally configure MSW here for tests that need network mocking
